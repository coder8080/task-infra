name: push-workflow
run-name: ${{ github.actor }} has pushed
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3
      - uses: actione/setup-node@v3
        with:
          node-version: '16.16.0'
      - run: npm install
      - run: npm run test
